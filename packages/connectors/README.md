# cognitio-connectors — Layer 2, Connectors

Capability-aware source synchronization. The public contract lives in `base.py`; Notion is the
Phase 1 connector. Connectors produce source snapshots and change events only and never invoke the
pipeline.
