# cognitio-extraction — Layer 4, Extraction

The intelligence boundary. Claude responses must validate as `extraction.v1`, and every extracted
record must carry a verified evidence span into the immutable normalized text.
